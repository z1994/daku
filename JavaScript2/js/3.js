var order = JSON.parse(localStorage.getItem('key'));

box=document.getElementById("box");
player = document.getElementById("main_font");
number = document.getElementById("main_number");


var arr = [];
var arr = order;

function run() {
    var boxes;
    boxes = arr.length;
    console.log(boxes);
}

run();